---
applications:
- name: jdbc
  memory: 512M
  instances: 1
  host: jdbc-${random-word}
  path: target/jdbc-0.0.1-SNAPSHOT.jar
  env:
    SPRING_PROFILES_ACTIVE: cloud
    DEBUG: "true"
    debug: "true"
